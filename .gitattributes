# Shell scripts must always use LF line endings
*.sh text eol=lf
*.py text eol=lf
*.md text eol=lf

# Avoid merge conflicts for sleepcode task tracking files
.sleepcode/task_queue.md -text merge=union
.sleepcode/task_done/*.md -text merge=union
